import { SpinnerContainer, StyledSpinner } from "./LoadingScreen.styled";

const LoadingScreen = () => {
  return (
    <SpinnerContainer>
      <StyledSpinner />
    </SpinnerContainer>
  );
};

export default LoadingScreen;
